# Required for LLM-based target extraction and embedding similarity calculation
OPENAI_API_KEY=

# Optional: Path to Google service account JSON file for Gemini-based extraction
# If not set, LLM extraction will use OpenAI only
GOOGLE_APPLICATION_CREDENTIALS=

# Optional: Google Cloud project ID (defaults to project from credentials file)
GOOGLE_PROJECT_ID=

# Optional: FinancialModelingPrep API key (only needed for src/fmp.py data download)
FMP_API_KEY=
